# 休息提醒工具 - 手机端安装说明

本工具提供两种手机端实现方案，您可以根据需求选择。

---

## 🌟 方案一：Kivy原生App（推荐）

**优点：**
- ✅ 真正的原生应用体验
- ✅ 支持后台运行
- ✅ 可添加到主屏幕
- ✅ 支持震动提醒

**缺点：**
- ❌ 需要打包编译
- ❌ 安装步骤相对复杂

### 安装步骤

#### 方法A：直接安装APK（Android）

1. **下载打包好的APK**
   - 文件名：`rest_reminder.apk`（需要先打包生成）

2. **在手机上安装**
   - 将APK文件传输到Android手机
   - 在手机设置中启用"允许安装未知来源应用"
   - 点击APK文件进行安装

3. **首次运行**
   - 打开应用，授予权限（如需要）
   - 设置提醒时间
   - 点击"开始提醒"

#### 方法B：从源码打包APK

**第一步：安装依赖**

在电脑上安装以下工具：

```bash
# 1. 安装 Python（建议 3.8+）
# 下载：https://www.python.org/downloads/

# 2. 安装 Kivy 和打包工具
pip install kivy
pip install buildozer
pip install cython
```

**第二步：安装打包依赖**

**Windows用户：**
```bash
# 安装 JDK 8+
# 下载：https://www.oracle.com/java/technologies/downloads/

# 安装 Android SDK
# 下载 Android Studio：https://developer.android.com/studio
```

**Linux/Mac用户：**
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install -y build-essential git python3 python3-pip
sudo apt install -y openjdk-8-jdk
sudo apt install -y automake libtool pkg-config libncurses5-dev libncursesw5-dev libtinfo5 libc6-dev lib32gcc1 lib32stdc++6

# macOS
brew install python3
brew install autoconf automake libtool pkg-config
brew install libtool
```

**第三步：打包APK**

```bash
# 进入项目目录
cd d:\project\休息提醒工具

# 初始化 buildozer（首次运行）
buildozer init

# 编辑 buildozer.spec 文件，设置：
# title = 休息提醒
# package.name = restreminder
# package.domain = org.myapp

# 开始打包（需要15-30分钟）
buildozer -v android debug

# 打包完成后，APK文件位于：
# bin/目录下
```

**第四步：安装到手机**

```bash
# 通过USB连接手机，启用USB调试
# 安装APK
adb install bin/restreminder-0.1-arm64-v8a-debug.apk

# 或者直接将APK文件复制到手机安装
```

---

## 🌐 方案二：Web应用（最简单）

**优点：**
- ✅ 无需安装，即开即用
- ✅ 跨平台（Android/iOS/电脑通用）
- ✅ 可添加到主屏幕像App一样使用
- ✅ 支持PWA（渐进式Web应用）

**缺点：**
- ❌ 需要保持浏览器打开
- ❌ 部分功能依赖浏览器支持

### 使用方法

#### 方法A：本地打开（测试用）

1. 在手机浏览器中打开 `rest_reminder_web.html`
2. 开始使用

#### 方法B：部署到服务器（推荐）

**选项1：使用 GitHub Pages（免费）**

1. **创建GitHub仓库**
   ```bash
   # 在GitHub创建新仓库，例如：rest-reminder
   ```

2. **上传文件**
   ```bash
   git init
   git add rest_reminder_web.html
   git commit -m "Add rest reminder web app"
   git branch -M main
   git remote add origin https://github.com/你的用户名/rest-reminder.git
   git push -u origin main
   ```

3. **启用 GitHub Pages**
   - 进入仓库设置
   - 找到 "Pages" 选项
   - Source 选择 "Deploy from a branch"
   - Branch 选择 "main/main"
   - 点击 Save

4. **访问应用**
   - 几分钟后访问：`https://你的用户名.github.io/rest-reminder/rest_reminder_web.html`

**选项2：使用 Vercel/Netlify（免费）**

1. 访问 [vercel.com](https://vercel.com) 或 [netlify.com](https://netlify.com)
2. 注册账号
3. 拖拽 `rest_reminder_web.html` 文件到页面
4. 获得访问链接

**选项3：本地服务器**

```bash
# 使用 Python 快速启动
python -m http.server 8000

# 手机和电脑连接同一WiFi
# 手机浏览器访问：http://电脑IP:8000/rest_reminder_web.html
```

#### 添加到主屏幕（像App一样）

**Android (Chrome):**
1. 打开应用网页
2. 点击菜单 (⋮)
3. 选择"添加到主屏幕"或"安装应用"

**iOS (Safari):**
1. 打开应用网页
2. 点击分享按钮
3. 选择"添加到主屏幕"

---

## 📱 依赖安装（Kivy版本）

如果Kivy版本提示缺少依赖，安装：

```bash
pip install kivy
pip install plyer  # 用于访问手机硬件（震动等）
```

或使用 requirements.txt：

```bash
pip install -r requirements.txt
```

---

## 🛠️ 常见问题

### Q: Kivy版本打包失败？
A:
1. 确保所有依赖正确安装
2. 查看错误日志，搜索解决方案
3. 考虑使用 Web 版本

### Q: Web版本无法震动？
A: 震动功能需要浏览器支持，且需在HTTPS环境下使用

### Q: 后台无法提醒？
A:
- Kivy版本：在手机设置中允许应用后台运行
- Web版本：建议保持浏览器标签页打开

### Q: 如何卸载？
A:
- APK版本：像普通应用一样卸载
- Web版本：从主屏幕删除，清除浏览器缓存

---

## 📋 文件清单

```
休息提醒工具/
├── rest_reminder.py              # 桌面版（Windows/Mac/Linux）
├── rest_reminder_mobile.py       # Kivy手机版（可打包APK）
├── rest_reminder_web.html        # Web版（跨平台）
├── 启动休息提醒.bat              # Windows启动脚本
├── create_shortcut.py            # 创建桌面快捷方式
├── requirements.txt              # Python依赖
└── 手机端安装说明.md            # 本文档
```

---

## 🎯 快速开始建议

**如果你想要最快体验：**
→ 使用 **Web版本**，直接在浏览器打开 `rest_reminder_web.html`

**如果你需要真正的App：**
→ 使用 **Kivy版本**，按照说明打包APK或使用预打包的APK

**如果你在电脑上使用：**
→ 直接运行 `rest_reminder.py` 或双击 `启动休息提醒.bat`

---

## 📞 技术支持

如遇到问题，请检查：
1. Python版本是否为3.6+
2. 所有依赖是否正确安装
3. 手机权限是否已授予
4. 浏览器是否支持所需功能

---

## 🔄 版本更新

- v1.0 - 初始版本
  - 支持定时提醒
  - 支持预设时间
  - 支持稍后提醒（贪睡模式）
  - 支持测试弹窗
  - 移动端优化
  - Web版本支持PWA

---

祝您使用愉快！按时休息，保护健康！💤
